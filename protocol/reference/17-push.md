# 服务端推送

> 31 个命令（cmd 26001 ~ 26049）。所有响应均以 1 字节 status 打头，成功值见各条目。

### `cmd=26001` — 推送：闪光提示（服务端推送）

**请求参数**: 无

**响应**（status 为 **1** 时成功；失败时仅 1 字节状态 + 错误文案字符串）:

| 顺序 | 类型 | 字段 | 说明 |
|---|---|---|---|
| 1 | u8 | `status` | 结果状态 |
| 2 | u32 | `type` | 类型枚举 |
| 3 | u8 | `level` | 等级 |

---

### `cmd=26002` — 推送：强制切城（服务端推送）

**请求参数**: 无

**响应**（status 为 **1** 时成功；失败时仅 1 字节状态 + 错误文案字符串）:

| 顺序 | 类型 | 字段 | 说明 |
|---|---|---|---|
| 1 | u8 | `status` | 结果状态 |
| 2 | u64 | `city_id` | 城池 ID |
| 3 | string | `message` | — |

---

### `cmd=26003` — 推送：动作追踪/活动提示（服务端推送）

**请求参数**: 无

**响应**: 无业务数据。status 为 **1** 时成功；失败时为状态字节 + 错误文案。

---

### `cmd=26004` — 推送：军团求助请求（服务端推送）

**请求参数**: 无

**响应**（status 为 **1** 时成功；失败时仅 1 字节状态 + 错误文案字符串）:

| 顺序 | 类型 | 字段 | 说明 |
|---|---|---|---|
| 1 | u8 | `flash_state` | — |

---

### `cmd=26005` — 推送：求助已被响应（服务端推送）

**请求参数**: 无

**响应**（status 为 **1** 时成功；失败时仅 1 字节状态 + 错误文案字符串）:

| 顺序 | 类型 | 字段 | 说明 |
|---|---|---|---|
| 1 | u8 | `status` | 结果状态 |

---

### `cmd=26006` — 推送：成员退团（服务端推送）

**请求参数**: 无

**响应**（status 为 **1** 时成功；失败时仅 1 字节状态 + 错误文案字符串）:

| 顺序 | 类型 | 字段 | 说明 |
|---|---|---|---|
| 1 | u8 | `status` | 结果状态 |

---

### `cmd=26007` — 推送：战斗发生（服务端推送）

**请求参数**: 无

**响应**（status 为 **1** 时成功；失败时仅 1 字节状态 + 错误文案字符串）:

> 含列表：先读计数字段，再按下列顺序循环读取每个条目。

| 顺序 | 类型 | 字段 | 说明 |
|---|---|---|---|
| 1 | u8 | `status` | 结果状态 |
| 2 | u64 | `expedition_id` | — |
| 3 | u32 | `expedition_type` | 类型枚举 |
| 4 | u32 | `tile_id` | — |
| 5 | u32 | `tile_x` | 地图 X 坐标 |
| 6 | u32 | `tile_y` | 地图 Y 坐标 |
| 7 | u8 | `quick_battle` | — |

---

### `cmd=26008` — 推送：战斗结果（服务端推送）

**请求参数**: 无

**响应**（status 为 **1** 时成功；失败时仅 1 字节状态 + 错误文案字符串）:

> 含列表：先读计数字段，再按下列顺序循环读取每个条目。

| 顺序 | 类型 | 字段 | 说明 |
|---|---|---|---|
| 1 | u8 | `status` | 结果状态 |
| 2 | u32 | `battle_result` | — |
| 3 | u64 | `expedition_id` | — |
| 4 | u32 | `expedition_type` | 类型枚举 |
| 5 | u32 | `tile_id` | — |
| 6 | u32 | `tile_x` | 地图 X 坐标 |
| 7 | u32 | `tile_y` | 地图 Y 坐标 |
| 8 | u8 | `tile_kind` | — |
| 9 | u8 | `target_type` | 类型枚举 |

---

### `cmd=26009` — 推送：军团战准备（服务端推送）

**请求参数**: 无

**响应**（status 为 **1** 时成功；失败时仅 1 字节状态 + 错误文案字符串）:

| 顺序 | 类型 | 字段 | 说明 |
|---|---|---|---|
| 1 | u8 | `status` | 结果状态 |
| 2 | u64 | `alliance_war_start_time` | 开始时间戳 |
| 3 | u64 | `alliance_war_end_time` | 军团 |

---

### `cmd=26010` — 推送：战斗消息一（服务端推送）

**请求参数**: 无

**响应**（status 为 **1** 时成功；失败时仅 1 字节状态 + 错误文案字符串）:

| 顺序 | 类型 | 字段 | 说明 |
|---|---|---|---|
| 1 | u8 | `status` | 结果状态 |
| 2 | string | `message` | — |

---

### `cmd=26011` — 推送：战斗消息二（服务端推送）

**请求参数**: 无

**响应**（status 为 **1** 时成功；失败时仅 1 字节状态 + 错误文案字符串）:

| 顺序 | 类型 | 字段 | 说明 |
|---|---|---|---|
| 1 | u8 | `status` | 结果状态 |
| 2 | string | `message` | — |

---

### `cmd=26012` — 推送：军团标记更新（服务端推送）

**请求参数**: 无

**响应**（status 为 **1** 时成功；失败时仅 1 字节状态 + 错误文案字符串）:

> 含列表：先读计数字段，再按下列顺序循环读取每个条目。

| 顺序 | 类型 | 字段 | 说明 |
|---|---|---|---|
| 1 | u8 | `status` | 结果状态 |
| 2 | u32 | `tile_x` | 地图 X 坐标 |
| 3 | u32 | `tile_y` | 地图 Y 坐标 |
| 4 | string | `title` | — |
| 5 | string | `icon` | 图标编号 |
| 6 | u64 | `time` | 时间戳（毫秒） |

---

### `cmd=26013` — 推送：军团标记删除（服务端推送）

**请求参数**: 无

**响应**（status 为 **1** 时成功；失败时仅 1 字节状态 + 错误文案字符串）:

| 顺序 | 类型 | 字段 | 说明 |
|---|---|---|---|
| 1 | u8 | `status` | 结果状态 |
| 2 | u32 | `tile_x` | 地图 X 坐标 |
| 3 | u32 | `tile_y` | 地图 Y 坐标 |

---

### `cmd=26014` — 推送：成员入团（服务端推送）

**请求参数**: 无

**响应**（status 为 **1** 时成功；失败时仅 1 字节状态 + 错误文案字符串）:

| 顺序 | 类型 | 字段 | 说明 |
|---|---|---|---|
| 1 | u8 | `status` | 结果状态 |
| 2 | u32 | `alliance_id` | 军团 ID |

---

### `cmd=26015` — 推送：在线校验（服务端推送）

**请求参数**: 无

**响应**（status 为 **1** 时成功；失败时仅 1 字节状态 + 错误文案字符串）:

| 顺序 | 类型 | 字段 | 说明 |
|---|---|---|---|
| 1 | u8 | `status` | 结果状态 |
| 2 | u64 | `current_time` | 时间戳（毫秒） |
| 3 | u64 | `show_time` | 时间戳（毫秒） |
| 4 | string | `sign` | — |

---

### `cmd=26016` — 推送：触发支付更新（服务端推送）

**请求参数**: 无

**响应**: 无业务数据。status 为 **1** 时成功；失败时为状态字节 + 错误文案。

---

### `cmd=26019` — 推送：活动更新（服务端推送）

**请求参数**: 无

**响应**（status 为 **1** 时成功；失败时仅 1 字节状态 + 错误文案字符串）:

| 顺序 | 类型 | 字段 | 说明 |
|---|---|---|---|
| 1 | u8 | `status` | 结果状态 |

---

### `cmd=26022` — 推送：调度功能 key（服务端推送）

> 实测修正：登录后服务器主动推送，body = 8 字节纯 u64 key（无额外 status 前缀，
> 推送帧 status 位置即 key 高字节）。会话期间 key 会刷新（实测约 1 分钟一换，每次刷新重新推送）。
> **`cmd=19003` 执行远征的最后一个参数必须填最新收到的 26022 key**，否则服务器拒绝（status=-1）。

**请求参数**: 无

**响应**（status 为 **1** 时成功；失败时仅 1 字节状态 + 错误文案字符串）:

| 顺序 | 类型 | 字段 | 说明 |
|---|---|---|---|
| 1 | u8 | `status` | 结果状态 |
| 2 | u64 | `key` | 调度 key，供 19003 远征帧使用 |

---

### `cmd=26023` — 推送：计谋功能 key（服务端推送）

**请求参数**: 无

**响应**（status 为 **1** 时成功；失败时仅 1 字节状态 + 错误文案字符串）:

| 顺序 | 类型 | 字段 | 说明 |
|---|---|---|---|
| 1 | u8 | `status` | 结果状态 |
| 2 | u64 | `key` | — |

---

### `cmd=26024` — 推送：伤兵更新（服务端推送）

**请求参数**: 无

**响应**: 无业务数据。status 为 **1** 时成功；失败时为状态字节 + 错误文案。

---

### `cmd=26025` — 推送：逃跑更新（服务端推送）

**请求参数**: 无

**响应**: 无业务数据。status 为 **1** 时成功；失败时为状态字节 + 错误文案。

---

### `cmd=26026` — 推送：演习战创建（服务端推送）

**请求参数**: 无

**响应**（status 为 **1** 时成功；失败时仅 1 字节状态 + 错误文案字符串）:

| 顺序 | 类型 | 字段 | 说明 |
|---|---|---|---|
| 1 | u8 | `status` | 结果状态 |
| 2 | u64 | `simulation_battle_id` | — |

---

### `cmd=26027` — 推送：演习战场摧毁（服务端推送）

**请求参数**: 无

**响应**（status 为 **1** 时成功；失败时仅 1 字节状态 + 错误文案字符串）:

| 顺序 | 类型 | 字段 | 说明 |
|---|---|---|---|
| 1 | u8 | `status` | 结果状态 |
| 2 | u32 | `battle_result` | — |

---

### `cmd=26041` — 推送：在线校验二（服务端推送）

**请求参数**: 无

**响应**（status 为 **1** 时成功；失败时仅 1 字节状态 + 错误文案字符串）:

| 顺序 | 类型 | 字段 | 说明 |
|---|---|---|---|
| 1 | u8 | `status` | 结果状态 |
| 2 | u64 | `current_time` | 时间戳（毫秒） |
| 3 | u64 | `show_time` | 时间戳（毫秒） |
| 4 | string | `sign` | — |
| 5 | u8 | `type` | 类型枚举 |
| 6 | u32 | `offset_y` | 地图 Y 坐标 |

---

### `cmd=26042` — 推送：战场玩法状态（服务端推送）

**请求参数**: 无

**响应**（status 为 **1** 时成功；失败时仅 1 字节状态 + 错误文案字符串）:

| 顺序 | 类型 | 字段 | 说明 |
|---|---|---|---|
| 1 | u64 | `current_time` | 时间戳（毫秒） |
| 2 | u64 | `start_time` | 开始时间戳 |
| 3 | u64 | `register_time` | 时间戳（毫秒） |
| 4 | u64 | `match_time` | 时间戳（毫秒） |
| 5 | u64 | `battle_time` | 时间戳（毫秒） |
| 6 | u64 | `end_time` | 结束时间戳 |
| 7 | u8 | `server_status` | 结果状态 |
| 8 | u64 | `register_time_duration` | 时间戳（毫秒） |
| 9 | u64 | `match_waiting_time` | 时间戳（毫秒） |
| 10 | u32 | `min_army_required` | — |
| 11 | u8 | `ranking_required` | 名次 |

---

### `cmd=26043` — 推送：战场玩法战斗（服务端推送）

**请求参数**: 无

**响应**（status 为 **1** 时成功；失败时仅 1 字节状态 + 错误文案字符串）:

| 顺序 | 类型 | 字段 | 说明 |
|---|---|---|---|
| 1 | u64 | `battle_id` | — |
| 2 | u8 | `battle_round` | — |

---

### `cmd=26044` — 推送：会话 accessToken（服务端推送）

**请求参数**: 无

**响应**（status 为 **1** 时成功；失败时仅 1 字节状态 + 错误文案字符串）:

| 顺序 | 类型 | 字段 | 说明 |
|---|---|---|---|
| 1 | string | `access_token` | 推送/会话凭据 |

---

### `cmd=26046` — 推送：远征信息更新（服务端推送）

**请求参数**: 无

**响应**（status 为 **1** 时成功；失败时仅 1 字节状态 + 错误文案字符串）:

| 顺序 | 类型 | 字段 | 说明 |
|---|---|---|---|
| 1 | u64 | `exp_id` | — |

---

### `cmd=26047` — 推送：系统 toast（服务端推送）

**请求参数**: 无

**响应**（status 为 **1** 时成功；失败时仅 1 字节状态 + 错误文案字符串）:

| 顺序 | 类型 | 字段 | 说明 |
|---|---|---|---|
| 1 | string | `message` | — |

---

### `cmd=26048` — 推送：玩家信息更新（服务端推送）

**请求参数**: 无

**响应**: 无业务数据。status 为 **1** 时成功；失败时为状态字节 + 错误文案。

---

### `cmd=26049` — 推送：地图格子更新（服务端推送）

**请求参数**: 无

**响应**（status 为 **1** 时成功；失败时仅 1 字节状态 + 错误文案字符串）:

| 顺序 | 类型 | 字段 | 说明 |
|---|---|---|---|
| 1 | u32 | `x` | 地图 X 坐标 |
| 2 | u32 | `y` | 地图 Y 坐标 |
