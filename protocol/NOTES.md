# 协议逆向全记录

> 本文件是协议知识的**唯一数据源**：帧格式、加密算法、参数编码、命令语义、初始化流程与分析防错指南。
> 配套文件：`commands.json`（命令字典）、`lib/w2build.js`（帧构造实现）。
> 所有结论均经真实抓包与回放代码闭环验证。

## 0. 总览

游戏客户端（iOS / Android 双端实现）与业务服务器之间走一条长连接 TCP 端口（默认 8083）。
传输层协议对上层透明，帧格式两端统一。

```
客户端 → 服务器   "WiST" 帧（请求，业务参数经 AES 加密）
服务器 → 客户端   "WIST" 帧（响应与推送，明文）
```

| 模块 | 状态 | 规范与实现 |
|---|---|---|
| 出站帧格式 / 加密算法 | ✅ 完全解析 | AES-128-ECB + MD5 前 16 字节截断（`lib/w2build.js`） |
| 入站帧格式 / 响应结构 | ✅ 完全解析 | 4B 长度前缀 UTF-8 字符串 + 整数混编（`lib/w2.js`） |
| 任意命令组帧构造 | ✅ 完全支持 | 支持凭空构造任意参数请求帧，无需依赖抓包重放 |
| 凭据重放与长连接认证 | ✅ 稳定可用 | 提取握手 wst token 即可脱离客户端维持业务会话 |
| 自动化任务领取 | ✅ 稳定可用 | 每日自动领取职位工资、军衔补助、钻石奖励（`scripts/w2signin.js`） |

---

## 1. 出站帧格式（客户端 → 服务器，"WiST"）

变长二进制帧，大端字节序：

```
偏移  长度  类型    字段说明
+0    4    ASCII   魔数，固定 "WiST"
+4    1    u8      协议版本，固定 0x02
+5    4    u32     序号 no（单连接内单调递增，自 1 开始）
+9    16   bytes   MD5 校验值（输入为：序号 + sessionId + 命令字 + 密文，取前 16 字节）
+25   4    u32     sessionId（客户端随机会话 ID，0 ~ 2^31-1，作为 AES 密钥种子）
+29   4    u32     长度 L（等于 密文长度 + 4）
+33   4    u32     命令字 cmd
+37   L-4  bytes   AES-128-ECB 密文（明文为命令字后面的业务参数，不含命令字）
```

## 2. 帧加密与校验算法

出站帧以 `sessionId` 为核心计算密钥与签名：

### ① 业务参数 AES 加密
- **算法**：AES-128-ECB
- **填充**：PKCS7
- **密钥推导**：`String(sessionId).padStart(16, '0')`（即十进制字符串左补零至 16 字符的 ASCII 字节）。
  - 例：`sessionId = 88888` → 密钥为 `"0000000000088888"`
- **明文输入**：紧随命令字之后的参数序列（空参命令则明文长度为 0，加密后为 16 字节填充块）。
- **密文输出**：原始二进制字节流。

### ② MD5 校验值计算
- **输入序列**：`u32BE(序号) ++ u32BE(sessionId) ++ u32BE(命令字) ++ AES密文`
- **截断规则**：计算标准 MD5 摘要（16 字节），取前 16 字节二进制原始值写入帧偏移 `+9` 处。

### ③ 组帧
将魔数、版本、序号、MD5 校验值、sessionId、长度 L、cmd 及 AES 密文顺序拼接即可。实现详见 `lib/w2build.js`。

---

## 3. 明文参数编码规范

明文参数位于 AES 加密块内，所有多字节数值均为**大端序（Big-Endian）**：

| 类型 | 长度 | 编码规则 |
|---|---|---|
| `byte` | 1B | 单字节整数（8 位） |
| `short` | 2B | 16 位整数（`writeShort`） |
| `int` | 4B | 32 位整数（`writeInt`） |
| `long` | 8B | 64 位整数（`writeLong`，玩家 ID、城池 ID、建筑 ID、时间戳均为此类型） |
| `string` | 变长 | 4 字节大端字节长度前缀 + UTF-8 原始字节（长度是字节数，非字符数） |

常用请求命令明文参数对照：

| cmd | 命令名称 | 明文参数定义 |
|---|---|---|
| `6` | 握手配置 hello | 空参数（明文长度 0B，仅有命令字） |
| `1001` | 账号登录 | `u64 userId` + `str username` + `u32 clientVer` + `str platform` + `str channel` + `str language` + `str appKey` + `str wst` + `str installID` |
| `10001` | 任务列表查询 | `byte taskType`（1 字节分类枚举，见 §6） |
| `10003` | 领取任务奖励 | `u32 taskId`（4 字节任务 ID） |
| `10006` | 新手任务列表 | `byte 4` |
| `6016` | 聊天记录拉取 | `u32 channelType` + `u64 fromChatId` + `byte earlierLoadMode` + `u32 loadMessageCount` |
| `12` | 推送 Token 注册 | `str token` + `byte deviceType` |

---

## 4. 入站帧格式（服务器 → 客户端，"WIST"）

入站帧为明文传输，无 AES 加密：

```
偏移  长度  类型    字段说明
+0    4    ASCII   魔数，固定 "WIST"
+4    4    u32     sessionId（回显请求帧的 sessionId；0xffffffff 表示服务端主动推送）
+8    4    u32     长度 L（等于 body 全长；body = 命令字(4B) + status(1B) + 数据）
+12   L    bytes   响应 Body：
                   - 前 4 字节：u32 命令字
                   - 第 5 字节：u8 status（0x01 表示成功；其余通常表示业务异常）
                   - 后续字段：根据命令字对应的响应结构反序列化（结构见 commands.json）
```

---

## 5. 核心约束与机制

1. **身份验证 = 连接级会话绑定**：协议没有 token/会话 ID 类字段。登录（1001）成功后，服务器将该 TCP 连接绑定到 userId，之后所有业务帧靠「这条连接是谁的」识别（帧内不含任何身份字段，实测未登录发业务请求返回 status=-2 拒绝）。因此**每条新连接必须先登录**，不存在「缓存凭据免登录」。
2. **单会话互斥**：同一账号仅允许一条活跃业务长连接，新连接登录成功即踢旧连接。脚本一次运行只建一个 socket。
3. **凭据长期有效**：`wst` 是登录参数中的长期凭据（SSO 下发），缓存它可让每次连接的登录动作都能成功；但它只是登录的入场券，不能省略登录。
4. **不防重放**：整帧原样重发长期有效（跨天验证过）；重复领取返回短响应但无副作用。
5. **随机会话密钥**：服务器按帧内 sessionId 推导 AES 密钥解密，客户端每帧可独立随机生成。
6. **连接只能登录一次**：同连接二次登录返回 status=-10（拒绝），SDK 的 connect() 已按此实现。
7. **频控**：客户端对同命令 500ms 内去重；服务器侧未见同类限制。

---

## 6. 任务系统分类（cmd=10001）

`cmd=10001` 请求参数为 1 字节 `taskType` 枚举：

| taskType | 分类名称 | 典型收录任务 |
|---|---|---|
| `0` | 日常军事 | 290（职位工资）、307（军衔补助）、5042（钻石奖励）、15138、50620 |
| `2` | 日常操作 | 15470（日常目标）、50620 |
| `3` | 兑换奖励 | 3448（绿色奖章兑换）、3764（迁城行动）、50628 |
| `4` | 名将兑换 | 11179（名将招募）、11200、11243 |

`w2signin.js` 采用直发 `10003` 领取帧模式，任务清单来自 `.env` 的 `W2_TASK_IDS`，不依赖 `10001` 列表返回结果（已领尽的分类返回空列表）。

---

## 7. 登录与初始化时序

客户端连接建立后的典型通信流程：

```
客户端                                      服务端
  │                                           │
  ├── 1. cmd=6 (hello, 空参) ────────────────>│
  │<── 2. 响应配置 (resourceHost/各类业务开关)──┤
  │                                           │
  ├── 3. cmd=1001 (登录, 带 wst 凭据) ────────>│
  │<── 4. 响应登录状态 (status=0x01) ─────────┤
  │<── 5. 推送 26001 / 26022 / 26044 (Token)──┤
  │                                           │
  ├── 6. 批量下发状态查询 (1005, 2001, 2003) ──>│
  │<── 7. 逐项返回核心资产数据 ────────────────┤
```

初始化关键响应数据速查：
- **`cmd=1005`**：玩家核心信息（钻石数量 `diamondOwned`、主城 ID `activeCityID`、城池总数、声望/战力、军衔等级、VIP）。
- **`cmd=8005`**：道具背包总表（包含当前钻石数以及全部背包道具的类型、ID、数量、名称）。
- **`cmd=2001`**：玩家全部城池清单（城池 ID、世界坐标 X/Y、城池名、驻守将领名与等级）。
- **`cmd=2003` / `cmd=2018`**：城内资源储量与生产效率（粮食、钢铁、石油、稀矿、黄金）。
- **`cmd=4001` / `cmd=4006`**：科技研究队列与科技原型字典。

---

## 8. 命令号分布规划

```
1 ~ 99        基础服务与心跳（1: 服务器时间、6: 配置 hello、11: 充值配置、12: 设备 Token）
1001 ~ 1999   玩家与账号系统（1001: 登录、1005: 核心信息/钻石、1015: 状态、1020: 黑名单、1030: 好友）
2001 ~ 2999   城池与资源系统（2001: 城池列表、2003: 资源储量、2018/2026: 生产效率）
3001 ~ 3999   军事与部队系统（3005: 训练队列、3008: 伤兵、3010: 逃跑、3014: 治疗）
4001 ~ 4999   科技研发系统（4001: 当前研究、4006: 原型字典）
5001 ~ 5999   军团系统（5001: 军团信息、5008: 军团列表、5062: 军团标记）
6001 ~ 6999   聊天系统（6001: 配置、6016: 消息列表、6020: 编号映射表＊）
8001 ~ 8999   道具与背包（8005: 背包总表、8016: 初始化查询＊、8020: 加速道具）
9001 ~ 9999   邮件系统（9001: 邮件列表、9002: 邮件正文）
10001 ~ 10999 任务与奖励系统（10001: 任务列表、10003: 领奖、10005: 分类、10006: 新手任务）
11001 ~ 11999 名将系统（11001: 招募列表、11002: 属性详情）
13002         游戏公告
17001 ~ 17999 建筑系统（17001: 军事建筑、17002: 生产建筑、17009: 建筑说明、17011: 原型表）
19005 ~ 19999 远征系统
21008         演习战
22001 ~ 22999 活动系统（22001: 活动列表、22002: 配置同步、22007: 充值活动、22016: 排行榜）
24001 ~ 24999 榜单排行（24011: 自身名次、24013: 积分榜、24016: 战力榜）
26001 ~ 26999 服务端广播推送（26001: 闪光、26003: 动作追踪/活动通知、26044: 会话凭据）
29003 ~ 29999 战场玩法（利刃之战等）
```

> ＊ 号命令（6020 / 8016）仅在 iOS 客户端抓包中出现，Android 客户端协议定义中未包含，
> 可能系版本迭代新增命令或平台实现差异，响应语义待解。

---

## 9. 扩展新功能标准流程

实现新业务操作（如自动造兵、自动研究、采集等）：

1. **确定命令号与结构**：
   查阅 [reference/](reference/README.md) 对应业务域文件的命令条目，确认请求参数字段与类型；reference 未覆盖的新命令，从客户端代码中定位该命令的协议类确认参数序列化顺序。
2. **使用构造器组帧**：
   调用 `lib/w2build.js` 中的 `buildFrame(no, sessionId, cmd, paramsBuffer)` 生成标准出站帧。
3. **注册字典与测试**：
   将命令定义录入 `protocol/commands.json`，通过 `scripts/w2probe.js` 或独立脚本发送，观察服务端入站响应（`status === 1` 即代表执行成功）。

---

## 10. 协议分析踩坑与防错指南

为避免后续分析或重构时重复踩坑，记录以下关键陷阱：

1. **密文勿当作固定尾或随机 Nonce**：
   出站帧偏移 `+37` 之后的数据全部是业务参数经 AES-128-ECB 加密后的密文。抓包时不同命令、不同参数的尾部数据变化，是因为明文参数不同；不要将其误判为签名 Nonce 或数据校验码。
2. **构造帧三要素缺一会导致服务端静默丢弃**：
   - MD5 校验输入必须严格按照 `u32(no) + u32(sessionId) + u32(cmd) + AES密文` 的顺序组合；
   - AES 密钥必须是 `sessionId` 转换为十进制后左侧补零至 16 字符的 ASCII 字节（不可当大端整数或十六进制解析）；
   - AES 必须使用 PKCS7 填充，且截断输出须为原始二进制，不可转 hex 字符串。
   以上任何一处不匹配，服务端均会**静默丢弃请求且不返回任何报错响应**。
3. **任务分类参数仅为 1 字节**：
   `cmd=10001` 的查询参数实质只是单字节 `taskType`。此前在抓包中看到的 16 字节看似“哈希串”的参数，实际上是该 1 字节参数经 AES 加密并填充后的密文块，切勿将其当成固定的 16 字节业务 ID 处理。
4. **大端序与 64 位整数（u64）**：
   所有网络序列均为大端序。特别是玩家 ID、城池 ID、建筑实例 ID、时间戳等均为 8 字节整数（对应客户端 `writeLong`/`readLong`），必须使用 `readBigUInt64BE` 读取，当作 32 位整数读取会导致高位截断或偏移错位。
5. **单会话互斥机制**：
   脚本连接登录后，在线的移动客户端会被强制下线；同样若在客户端点击重新登录，脚本的业务 Socket 会被服务端断开。测试或定时任务执行时必须考虑单会话互斥，避免双端频繁互相踢下线。
6. **软路由流量卸载（Flow Offloading）对抓包的影响**：
   在搭载 OpenWrt / Linux 的软路由上使用 `tcpdump` 抓包时，若开启了硬件/软件流量卸载（Flow Offload / Shortcut-FE / SFE），经过路由直通转发的 TCP 载荷包不会进入内核协议栈，导致只能抓到三次握手与挥手包，业务数据完全丢失。抓包前必须关闭流量卸载。
7. **客户端休眠断连**：
   移动设备息屏或切换后台时，系统会迅速挂起应用进程并切断长连接。抓包排查时必须确保测试设备屏幕常亮。
8. **勿因单平台未定义而删除抓包真实存在的命令**：
   部分命令（如 `6020` 编号映射表、`8016` 初始化查询）在 Android 客户端协议定义中未包含，
   但它们是 iOS 客户端真实发出并收到响应的命令。做字典收录 / 文档整理时，应以「抓包实据」为准保留记录，
   标注「语义未解 / 平台未定义」即可，不可当作不存在的命令删除——否则会让后续分析者误判为未知帧。
